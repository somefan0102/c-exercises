#ifndef STACK_H
#define STACK_H

// Push `value` to stack.
void push(double value);

// Pop and returns `value` from stack.
double pop(void);

// Returns top value from stack.
double top(void);

// Returns the pointer/length of stack.
int topp(void);

// Clears the stack.
void clear(void);

#endif